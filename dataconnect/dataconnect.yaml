specVersion: "v1"
serviceId: "campalpha"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "campalpha-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
